<?php
namespace {{ namespace }}\Migration\AbstractMigration;

class {{ name }}Migration extends AbstractMigration
{
    /**
     * Change Method.
     *
     * Add foreign keys.
     *
     * More information on this method is available here:
     * http://docs.phinx.org/en/latest/migrations.html#the-change-method
     * @return void
     */
    public function change()
    {
{% for table, data in foreignKeys %}
        $table = $this->table({{ table }});
{% for reference, data in data %}
        $table->addForeignKey('{{ data.foreignKey }}', '{{ reference }}', '{{data.primaryKey}}'); 
{% endfor %}

{% endfor %}
    }
}
